pipeline {
    agent any
    
    stages {
        stage('clone code') {
            steps {
                checkout scm
            }
        }
         stage('installing  dependancies') {
            steps {
                sh "install npm"
            }
        }
         stage('running app local') {
            steps {
                sh "node app.js"
            }
        }
    }
}